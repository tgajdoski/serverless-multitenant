import { AnimateChangeDirective } from './animate-change.directive';

describe('AnimateChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateChangeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
